# Template for .streamlit/secrets.toml (Streamlit Cloud Secrets / local dev).
# Copy this file to .streamlit/secrets.toml and fill in your own keys.
# The real secrets.toml is gitignored and must NEVER be committed.
#
# On Streamlit Cloud: Settings -> Secrets, paste the same TOML content below.

openrouter_api_key = "sk-or-..."
groq_api_key = "gsk_..."
opencode_zen_api_key = "sk-zen-..."